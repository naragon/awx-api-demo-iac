---
# Keep only safe defaults here.
# Connection/auth vars should be provided by inventories/group_vars/all/awx.yml
# or via CLI extra-vars/environment when running playbooks.
awx_validate_certs: false
awx_project_scm_branch: "main"
